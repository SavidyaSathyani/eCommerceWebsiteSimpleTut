import React from 'react';
import Directory from '../../components/Directory';
import './styles.scss'

const Home = props => {
  return (
    <section className="homepage">
      <Directory />
    </section>
  );
};

export default Home;
